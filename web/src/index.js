import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Diz para renderizar a aplicação React
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

